name: CI com GitHub Actions

on:
  push:
    branches:
      - "main"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_ACCESS_TOKEN }}

      - name: Configurar o Docker Buildx
        uses: docker/setup-buildx-action@v3
        # O Buildx ajuda a otimizar o cache das camadas do Docker no GitHub Actions

      - name: Construir e enviar a imagem Docker
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/minha-api-spring:latest
            ${{ secrets.DOCKER_USERNAME }}/minha-api-spring:${{ github.run_id }}



#name: CI-CD com github actions
#
#on:
#  push:
#    branches:
#      - "main"
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#
#    steps:
#    - uses: actions/checkout@v4
#
#    - name: Login to Docker Hub
#      uses: docker/login-action@v4
#      with:
#        username: ${{ secrets.DOCKER_USERNAME }}
#        password: ${{ secrets.DOCKER_ACCESS_TOKEN }}
#
#    - name: Setup Java 21
#      uses: actions/setup-java@v4
#      with:
#        java-version: '21'
#        distribution: 'temurin' # See 'Supported distributions' for available options
#
#    - name: Build Application .jar
#      working-directory: ./aws_test
#      run: mvn clean package
#
#    - name: Docker Compose Build
#      working-directory: ./aws_test
#      run: docker compose build
#
#    - name: Push to Docker Hub Container Registry
#      run:
#        docker tag ${{ secrets.DOCKER_USERNAME }}/minha-api-spring:latest ${{ secrets.DOCKER_USERNAME }}/minha-api-spring:${{ github.run_id }}
#
#        docker push ${{ secrets.DOCKER_USERNAME }}/minha-api-spring:latest
#        docker push ${{ secrets.DOCKER_USERNAME }}/minha-api-spring:${{ github.run_id }}